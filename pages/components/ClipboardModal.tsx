const ClipboardModal = () => {
    return (
        <div className=" text-base md:text-xl p-3 rounded-lg bg-darkGrey2 w-fit h-fit">
            jeremieemk@gmail.com was copied to your clipboard
        </div>
    );
};

export default ClipboardModal;